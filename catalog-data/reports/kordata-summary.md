# Resumen importacion Kordata STYLUS

Pendiente de generar. Guarda un archivo `.xlsx` exportado desde Kordata en `catalog-data/kordata/` y ejecuta:

```bash
npm run import:kordata
```

`data/products.json` no se modifica en este flujo.
